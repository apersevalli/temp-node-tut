// local
const secret = 'SUPER SECRET';
// share
const john = 'john';
const peter = 'peter';

//console.log(module);

// If we have multiple values to export, we can export
// as an object, and in the object we set the properties
module.exports = {john, peter}